pfSense package for managing the FRP client (frpc) from the pfSense web
interface. It installs a Services menu entry, writes frpc.toml from the GUI,
and controls the frpc daemon through an rc.d script.
